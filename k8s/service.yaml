apiVersion: v1
kind: Service
metadata:
  name: dividend-audit-service
  namespace: default
  labels:
    app: dividend-audit
spec:
  type: ClusterIP
  selector:
    app: dividend-audit
  ports:
  - name: http
    protocol: TCP
    port: 6768
    targetPort: 6768
